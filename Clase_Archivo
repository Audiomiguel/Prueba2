#pragma once
#include <string>
#include <iostream>
using namespace std;

class Archivo
{
private:
	string nombre;
	long long tamanio;
	string extension;
	string direccion;
	string fecha;
public:
	Archivo(string nombre, long long tamanio, string extension,
		string fecha, string direccion) 
		: nombre(nombre), tamanio(tamanio), extension(extension),
		direccion(direccion), fecha(fecha){};
	~Archivo(){}
	
	string getNombre() { return nombre; }
	long long getTamanio() { return tamanio; }
	string getExtension() { return extension; }
	string getDireccion() { return direccion; }
	string getFecha() { return fecha; }

};


